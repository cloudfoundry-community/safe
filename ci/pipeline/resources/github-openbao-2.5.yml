resources:
- name: github-openbao-2.5
  type: github-release
  check_every: 1h
  source:
    user:         openbao
    repository:   openbao
    access_token: (( grab meta.github.access_token ))
    tag_filter: v?(2.5.[^v].*)
